# Zkopíruj na serveru jako `.env` a vyplň.
# (.env je v .gitignore, takže se neverzuje.)

# ENTSO-E API token — vlastní token získáš na https://transparency.entsoe.eu
# Když nevyplníš, použije se výchozí token z config.py.
ENTSOE_TOKEN=
